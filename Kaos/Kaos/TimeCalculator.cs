using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Kaos
{
    public static class TimeCalculator
    {
        /// <summary>
        /// Returns the all-time delay of a transfer in minutes
        /// </summary>
        /// <returns></returns>
        public static double GetAllTimeTransferDelay(string transferId)
        {
            throw new NotImplementedException("I needs my DB guy to fix stuffs first ples");

            // Send call to DB routine
            // Adds together all delays with a sertain transfer id
        }
    }
}